export type Product = {
  id: number;
  name: string;
  code: string;
  description: string;
  price: number;
  salePrice: number;
  onSale: boolean;
  category: string;
  quantity: number;
  imageUrl: string;
}
